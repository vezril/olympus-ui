name: Release

on:
  push:
    tags: ["v*.*.*"]

permissions:
  contents: write

jobs:
  guard:
    name: tag guards
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      publish: ${{ steps.secrets.outputs.publish }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: parse version
        id: version
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME}"
          if [[ ! "$tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "FATAL: '$tag' is not vX.Y.Z" >&2
            exit 1
          fi
          echo "version=${tag#v}" >> "$GITHUB_OUTPUT"

      - name: tag must be an ancestor of main
        # Releases come off main only. A tag on a side branch would publish an
        # immutable image from code that was never merged.
        run: |
          set -euo pipefail
          # No --depth here: checkout already fetched full history (fetch-depth: 0),
          # and `--depth=0` is rejected outright ("depth 0 is not a positive number").
          git fetch origin main
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "FATAL: ${GITHUB_REF_NAME} (${GITHUB_SHA}) is not an ancestor of main" >&2
            exit 1
          fi
          echo "ok: on main"

      - name: chart version matches the tag
        run: |
          set -euo pipefail
          version="${{ steps.version.outputs.version }}"
          chart_version=$(grep -E '^version:' deploy/charts/olympus-ui/Chart.yaml | awk '{print $2}')
          app_version=$(grep -E '^appVersion:' deploy/charts/olympus-ui/Chart.yaml | awk '{print $2}' | tr -d '"')
          if [[ "$chart_version" != "$version" || "$app_version" != "$version" ]]; then
            echo "FATAL: tag $version vs chart $chart_version / appVersion $app_version" >&2
            exit 1
          fi
          echo "ok: chart pinned to $version"

      - name: are Docker Hub secrets present?
        id: secrets
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          if [[ -n "$DOCKERHUB_USERNAME" && -n "$DOCKERHUB_TOKEN" ]]; then
            echo "publish=true" >> "$GITHUB_OUTPUT"
          else
            echo "publish=false" >> "$GITHUB_OUTPUT"
            echo "::warning::DOCKERHUB_* secrets absent — skipping the image publish."
          fi

  publish:
    name: build · push
    needs: guard
    if: needs.guard.outputs.publish == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: semver immutability — refuse to overwrite a published tag
        env:
          VERSION: ${{ needs.guard.outputs.version }}
        run: |
          set -euo pipefail
          # Docker Hub account is `calvinference`, NOT the GitHub org name.
          url="https://hub.docker.com/v2/repositories/calvinference/olympusui/tags/${VERSION}"
          code=$(curl -sS -o /dev/null -w "%{http_code}" "$url")
          if [[ "$code" == "200" ]]; then
            echo "FATAL: calvinference/olympusui:${VERSION} already exists. Versions are immutable — cut a new one." >&2
            exit 1
          fi
          echo "ok: ${VERSION} is unpublished (HTTP $code)"

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            NEXT_PUBLIC_OLYMPUS_API_BASE=/api/olympus
          tags: |
            calvinference/olympusui:${{ needs.guard.outputs.version }}
            calvinference/olympusui:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  release:
    name: github release
    needs: [guard, publish]
    if: always() && needs.guard.result == 'success' && needs.publish.result != 'failure'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: create release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.guard.outputs.version }}
          PUBLISHED: ${{ needs.guard.outputs.publish }}
        run: |
          set -euo pipefail

          if [[ "$PUBLISHED" == "true" ]]; then
            image_line="Image: \`calvinference/olympusui:${VERSION}\`"
          else
            image_line="_No image published - DOCKERHUB_* secrets were not configured._"
          fi

          {
            echo "$image_line"
            echo
            echo "Chart: \`deploy/charts/olympus-ui\` @ ${VERSION}"
            echo
            echo "Deploy is manual (no Flux on k3s 1.21): \`helm upgrade\` from the"
            echo "TAGGED chart with the mirrored values file in codex \`apps/olympus/\`."
            echo "Never a bare \`--set\` - it replaces all user-supplied values."
          } > release-notes.md

          gh release create "$GITHUB_REF_NAME" \
            --title "$GITHUB_REF_NAME" \
            --generate-notes \
            --notes-file release-notes.md
