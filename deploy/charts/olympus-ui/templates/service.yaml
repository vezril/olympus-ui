apiVersion: v1
kind: Service
metadata:
  name: {{ include "olympus-ui.fullname" . }}
  labels:
    {{- include "olympus-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "olympus-ui.selectorLabels" . | nindent 4 }}
